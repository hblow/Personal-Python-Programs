======================
UTF Converter
======================
Converts a given .txt file from UTF-8 encoding to UTF-16.

======================
Future Plans
======================
1. Allow for any encoding to be chosen(or if that is not
possible, from a list of valid encoding options).
2. Perhaps allow for multiple files to be swapped in
encoding based on user input or maybe file input with
filenames.


